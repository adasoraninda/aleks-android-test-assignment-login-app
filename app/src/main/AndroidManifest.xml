<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adasoraninda.loginapplication">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginApplication">

        <activity
            android:name=".screens.SplashActivity"
            android:theme="@style/Theme.LoginApplicationSplash" />

        <activity
            android:name=".screens.Activity"
            android:windowSoftInputMode="stateVisible|adjustResize" />

        <activity-alias
            android:name="Launcher"
            android:exported="true"
            android:targetActivity=".screens.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

    </application>

</manifest>